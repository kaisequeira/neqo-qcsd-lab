\documentclass[tikz,border=0pt]{standalone}
\input{qcsd-diagram-style.tex}

\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm]
  \QCSDSvgMetadata
    {One paired visit across all seven QCSD selections}
    {A planned workload visit expands into seven independent direct-capture samples. Every sample has the same PCAP, trace, and fidelity contract. The assignment is owned by a source-manifest-and-repetition split group, so aliases and other derivatives cannot leak across partitions. A failed, drifting, or fidelity-ineligible sample excludes the paired visit from headline comparison while remaining in operational statistics.}
  \path[use as bounding box] (0,0) rectangle (15.5,7.15);

  \node[
    draw=qink,fill=white,line width=.7pt,rounded corners=2pt,
    minimum width=2.25cm,minimum height=1.10cm,align=center
  ] (visit) at (1.70,5.15) {
    \textbf{workload A}\\[-1pt]
    \scriptsize visit 3
  };
  \node[caption,font=\bfseries] at (1.70,6.12) {one planned visit};
  \node[
    draw=qink,fill=qpale,line width=.6pt,rounded corners=1.5pt,
    minimum width=2.75cm,minimum height=.48cm,align=center
  ] at (1.70,3.90) {\scriptsize split group: \textbf{70 / 15 / 15}};
  \node[tiny note,align=center,text width=3.0cm] at (1.70,3.10)
    {Keyed by manifest hash + repetition;\\never alias, policy, scope, or defence.};

  \node[
    draw=qink,fill=white,line width=.7pt,rounded corners=2pt,
    minimum width=3.35cm,minimum height=4.55cm,
    align=left,text width=2.95cm
  ] (loads) at (5.12,4.05) {
    \centering\textbf{seven independent loads}\\[5pt]
    \scriptsize Undefended\\
    Static\\
    FRONT\\
    Tamaraw\\
    Traffic Morphing\\
    WTF-PAD\\
    Walkie-Talkie\\[6pt]
    \centering\tiny one direct PCAP + trace each\\
    same artifact paths for\\simple / complex
  };
  \draw[flow] (visit.east) -- (loads.west |- visit.east);

  \node[
    draw=qink,fill=white,line width=.7pt,rounded corners=2pt,
    minimum width=2.20cm,minimum height=1.45cm,
    align=center,text width=1.95cm
  ] (validate) at (8.22,4.05) {
    \textbf{validate}\\[3pt]
    \scriptsize endpoint-tuple union\\
    PCAP = trace\\
    response complete\\
    fidelity record
  };
  \draw[flow] (loads.east) -- (validate.west);

  \node[
    draw=qink,fill=qpale,line width=.7pt,rounded corners=2pt,
    minimum width=2.95cm,minimum height=2.35cm,
    align=center,text width=2.60cm
  ] (pair) at (11.28,4.05) {
    \textbf{paired}\\[-1pt]\textbf{comparison}\\[4pt]
    \scriptsize two classic plot pages\\
    paired overhead\\table\\
    classifier index view\\[4pt]
    \tiny all seven must be eligible
  };
  \draw[flow] (validate.east) -- (pair.west);

  \pic[scale=1.02] at (14.14,4.05) {classifier};
  \node[small note,align=center] at (14.14,3.20)
    {one attacker\\per defence};
  \draw[flow] (pair.east) -- (13.58,4.05);

  \draw[failure] (8.22,3.22) -- (8.22,1.32) -- (9.08,1.32);
  \node[caption,text=qred,anchor=west,align=left,text width=5.5cm] at (9.18,1.58)
    {Any failure, content drift, or invalid direct trace:};
  \node[small note,anchor=west,align=left,text width=5.5cm] at (9.18,.98)
    {exclude the pair from headline comparison; retain the event in operational statistics.};
\end{tikzpicture}
\end{document}
